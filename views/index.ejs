<!DOCTYPE html>
<html>
  <head>
    <title>RCA Gen</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">
  </head>
  <body>

    
    <div class="container">
      <div class="row">
        <h3>Old RCAs</h3>
        
        <ul>

          <% if(result != '') { %>

          <% result.forEach(function(rca) { %>
            <a href="<%= rca.fullUrl %>/view/<%= rca.ser_num %>"><%= rca.date %> : <%= rca.ser_num %></a><br>
          <% }); %> 

          <% }else{ %> 
            <p>You don't have any RCAs yet.</p>
          <% } %> 

         </ul>
      </div>
      <br><br>

      <div class="row" style="margin-right: 10px;margin-left: 10px;">
        <div class="col-lg-12">


          <form action="/gen" method="GET">
            <div class="mb-3">
              <label for="date" class="form-label">What's the Date of the incident ?</label>
              <input required type="date" class="form-control" name="date">
            </div>

            <div class="mb-3">
              <label class="form-label">BPO Sites Affected ?</label>
              <textarea required name="bpo" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label for="inc" class="form-label">What is the incident number ?</label>
              <textarea required name="inc" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">Prepared By ?</label>
              <textarea required name="prepared" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">What is the start date & time?</label>
              <textarea required name="start_time" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">What is the end date & time?</label>
              <textarea required name="end_time" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">What is the problem description ?</label>
              <textarea required name="desc" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">Who is impacted ?</label>
              <textarea required name="impacted" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">Number of Workstation ?</label>
              <textarea required name="num_of_workstations" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">What is the root cause ?</label>
              <textarea required name="root_cause" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">What is the resolution ?</label>
              <textarea required name="resl" class="form-control"></textarea>
            </div>

            <div class="mb-3">
              <label class="form-label">What is the prevention ?</label>
              <textarea required name="preven" class="form-control"></textarea>
            </div>

            <!-- <input type="file" name="avatar" /> -->
            
            <button type="submit" class="btn btn-primary">Submit</button>
          </form>


        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.6.0/dist/umd/popper.min.js" integrity="sha384-KsvD1yqQ1/1+IA7gi3P0tyJcT3vR+NdBTt13hSJ2lnve8agRGXTTyNaBYmCR/Nwi" crossorigin="anonymous"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/js/bootstrap.min.js" integrity="sha384-nsg8ua9HAw1y0W1btsyWgBklPnCUAFLuTMS2G72MMONqmOymq585AcH49TLBQObG" crossorigin="anonymous"></script>
  </body>
</html>
